<UserControl
    x:Class="EpsilonDesktop.Views.JogAxesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:axis="clr-namespace:EpsilonCore.Motion.Axis;assembly=EpsilonCore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EpsilonDesktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:project="clr-namespace:EpsilonDesktop"
    xmlns:models="clr-namespace:EpsilonDesktop.Models"
    xmlns:viewModels="clr-namespace:EpsilonDesktop.ViewModels"
    x:Name="RootWindow"
    d:DataContext="{d:DesignInstance Type=viewModels:JogAxesViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListBox
            Grid.Row="0"
            Grid.Column="0"
            Width="200"
            Margin="10"
            ItemsSource="{Binding DistancesLinear}"
            SelectedItem="{Binding SelectedDistanceLinear}"
            Visibility="{Binding Axis1Visibility}" />

        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Y Up Arrow (↑)  -->
            <Button
                Grid.Row="0"
                Grid.Column="1"
                Width="60"
                Height="60"
                Content="{x:Static project:Constants.ARROW_UP}"
                FontSize="20"
                IsEnabled="{Binding Axis2Enabled}"
                Visibility="{Binding Axis2Visibility}" 
                Command="{Binding Axis2ForwardsCommand}"/>

            <!--  X Left Arrow (←)  -->
            <Button
                Grid.Row="1"
                Grid.Column="0"
                Width="60"
                Height="60"
                Content="{x:Static project:Constants.ARROW_LEFT}"
                FontSize="20"
                IsEnabled="{Binding Axis1Enabled}"
                Visibility="{Binding Axis1Visibility}" 
                Command="{Binding Axis1BackwardsCommand}"/>

            <!--  X Right Arrow (→)  -->
            <Button
                Grid.Row="1"
                Grid.Column="2"
                Width="60"
                Height="60"
                Content="{x:Static project:Constants.ARROW_RIGHT}"
                FontSize="20"
                IsEnabled="{Binding Axis1Enabled}"
                Visibility="{Binding Axis1Visibility}" 
                Command="{Binding Axis1ForwardsCommand}"/>

            <!--  Y Down Arrow (↓)  -->
            <Button
                Grid.Row="2"
                Grid.Column="1"
                Width="60"
                Height="60"
                Content="{x:Static project:Constants.ARROW_DOWN}"
                FontSize="20"
                IsEnabled="{Binding Axis2Enabled}"
                Visibility="{Binding Axis2Visibility}" 
                Command="{Binding Axis2BackwardsCommand}"/>

            <StackPanel
                Orientation="Horizontal"
                Grid.Row="1"
                Grid.Column="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Label
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="16"
                    Content="{Binding Axis1.Name}">
                </Label>
                <Label
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="16"
                    Content="/">
                </Label>
                <Label
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="16"
                    Content="{Binding Axis2.Name}">
                </Label>
            </StackPanel>
            

            <!--  Z Up Arrow (↑)  -->
            <Button
                Grid.Row="0"
                Grid.Column="3"
                Width="60"
                Height="60"
                Margin="20,0"
                Content="{x:Static project:Constants.ARROW_UP}"
                FontSize="20"
                IsEnabled="{Binding Axis3Enabled}"
                Visibility="{Binding Axis3Visibility}" 
                Command="{Binding Axis3ForwardsCommand}"/>

            <Label
                Grid.Row="1"
                Grid.Column="3"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="{Binding Axis3.Name}"
                FontSize="16"/>

            <!--  Z Down Arrow (↓)  -->
            <Button
                Grid.Row="2"
                Grid.Column="3"
                Width="60"
                Height="60"
                Margin="20,0"
                Content="{x:Static project:Constants.ARROW_DOWN}"
                FontSize="20"
                IsEnabled="{Binding Axis3Enabled}"
                Visibility="{Binding Axis3Visibility}" 
                Command="{Binding Axis3BackwardsCommand}"/>

            <Button
                Grid.Row="0"
                Grid.Column="4"
                Margin="20,10,10,10"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Content="Home All"
                FontSize="20"
                IsEnabled="{Binding HomeAllEnabled}"
                Visibility="{Binding HomeAllVisibility}" 
                Command="{Binding HomeAllCommand}"/>
            
            <Button
                Grid.Row="1"
                Grid.Column="4"
                Margin="20,10,10,10"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Content="{Binding Axis1.Name, StringFormat='Home {0}'}"
                FontSize="20"
                IsEnabled="{Binding Axis1Enabled}"
                Visibility="{Binding Axis1Visibility}" 
                Command="{Binding HomeAxis1Command}"
                MinWidth="40"/>

            <Button
                Grid.Row="2"
                Grid.Column="4"
                Margin="20,10,10,10"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Content="{Binding Axis2.Name, StringFormat='Home {0}'}"
                FontSize="20"
                IsEnabled="{Binding Axis2Enabled}"
                Visibility="{Binding Axis2Visibility}" 
                Command="{Binding HomeAxis2Command}"
                MinWidth="40"/>

            <Button
                Grid.Row="3"
                Grid.Column="4"
                Margin="20,10,10,10"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Content="{Binding Axis3.Name, StringFormat='Home {0}'}"
                FontSize="20"
                IsEnabled="{Binding Axis3Enabled}"
                Visibility="{Binding Axis3Visibility}" 
                Command="{Binding HomeAxis3Command}"
                MinWidth="40"/>

            <!--  Other Linear Axis Buttons  -->
            <ItemsControl
                Grid.Row="4"
                Grid.Column="0"
                Grid.ColumnSpan="5"
                ItemsSource="{Binding OtherAxesLinear}"
                HorizontalAlignment="Left">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="viewModels:AxisLinearButtonsViewModel">
                        <Grid HorizontalAlignment="Left">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!--  Negative Direction Arrow (←)  -->
                            <Button
                                Grid.Row="0"
                                Grid.Column="0"
                                Width="60"
                                Height="60"
                                Command="{Binding MoveAxisBackwardLinearCommand, ElementName=RootWindow}"
                                CommandParameter="{Binding Axis}"
                                Content="{x:Static project:Constants.ARROW_LEFT}"
                                FontSize="20"
                                IsEnabled="{Binding ButtonsEnabled}" />

                            <!--  fake hidden button (this is just to take up space to line up with other buttons  -->
                            <Button
                                Grid.Row="0"
                                Grid.Column="1"
                                Width="60"
                                Height="60"
                                FontSize="20"
                                Visibility="Hidden" />

                            <!--  Positive Direction Arrow (→)  -->
                            <Button
                                Grid.Row="0"
                                Grid.Column="2"
                                Width="60"
                                Height="60"
                                Command="{Binding MoveAxisForwardLinearCommand, ElementName=RootWindow}"
                                CommandParameter="{Binding Axis}"
                                Content="{x:Static project:Constants.ARROW_RIGHT}"
                                FontSize="20"
                                IsEnabled="{Binding ButtonsEnabled}" />

                            <!--  fake hidden button (this is just to take up space to line up with other buttons  -->
                            <Button
                                Grid.Row="0"
                                Grid.Column="3"
                                Width="60"
                                Height="60"
                                FontSize="20"
                                Visibility="Hidden" 
                                Margin="20,0"/>

                            <!--  Home Button  -->
                            <Button
                                Grid.Row="0"
                                Grid.Column="4"
                                Margin="20,10,10,10"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="{Binding Axis.Name, StringFormat='Home {0}'}"
                                FontSize="20"
                                IsEnabled="{Binding ButtonsEnabled}" 
                                MinWidth="40"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <!--  Rotational Distance Options  -->
        <ListBox
            Grid.Row="1"
            Grid.Column="0"
            Width="200"
            Margin="10"
            ItemsSource="{Binding DistancesRotational}"
            SelectedItem="{Binding SelectedDistanceRotational}" 
            Visibility="{Binding RotationalDistancesVisibility}" />

        <!--  Rotational Axis Buttons  -->
        <ItemsControl
            Grid.Row="1"
            Grid.Column="1"
            ItemsSource="{Binding AllAxesRotational}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="viewModels:AxisRotationalButtonsViewModel">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!--  Clockwise (Negative) Arrow (↷)  -->
                        <Button
                            Grid.Row="0"
                            Grid.Column="1"
                            Width="60"
                            Height="60"
                            Command="{Binding MoveAxisForwardRotationalCommand, ElementName=RootWindow}"
                            CommandParameter="{Binding Axis}"
                            Content="{x:Static project:Constants.ARROW_CLOCKWISE}"
                            FontSize="20"
                            IsEnabled="{Binding ButtonsEnabled}" />


                        <!--  fake hidden button (this is just to take up space to line up with other buttons  -->
                        <Button
                            Grid.Row="0"
                            Grid.Column="1"
                            Width="60"
                            Height="60"
                            FontSize="20"
                            Visibility="Hidden" />

                        <!--  Counterclockwise (Positive) Arrow (↶)  -->
                        <Button
                            Grid.Row="0"
                            Grid.Column="2"
                            Width="60"
                            Height="60"
                            Command="{Binding MoveAxisBackwardRotationalCommand, ElementName=RootWindow}"
                            CommandParameter="{Binding Axis}"
                            Content="{x:Static project:Constants.ARROW_COUNTERCLOCKWISE}"
                            FontSize="20"
                            IsEnabled="{Binding ButtonsEnabled}" />

                        <!--  Home Button  -->
                        <Button
                            Grid.Row="0"
                            Grid.Column="3"
                            Margin="20,10,10,10"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Content="{Binding Axis.Name, StringFormat='Home {0}'}"
                            FontSize="20"
                            IsEnabled="{Binding ButtonsEnabled}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
