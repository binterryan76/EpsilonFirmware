<UserControl
    x:Class="EpsilonDesktop.Views.FilePickerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EpsilonDesktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:EpsilonDesktop.Models"
    xmlns:viewModels="clr-namespace:EpsilonDesktop.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewModels:FilePickerViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Label
            Grid.Row="0"
            Grid.Column="0"
            Margin="5,5,5,0"
            Content="{Binding LabelText}" />
        <TextBox
            Grid.Row="1"
            Grid.Column="0"
            MaxHeight="200"
            Margin="5,0,5,5"
            AcceptsReturn="True"
            Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap" />
        <Button
            Grid.Row="1"
            Grid.Column="1"
            Padding="5"
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Command="{Binding BrowseCommand}"
            Content="Browse..." />
    </Grid>
</UserControl>


